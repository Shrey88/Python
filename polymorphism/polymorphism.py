"""
    poly means many
    morphe means form

    The ability of objects to have different forms is called polymorphism which just means many forms

    below objects are of different types which behave differently but they have same behavior that is printable.

    __str__ method just returns the name of the class and a hash code or the address in memory where the object lives.
"""

""" a is an int and we can subtract two ints to work out their differences """
a = 3

""" b is a string, they are different types of object, you can't subtract strings in Python """
b = "tim"

""" c is a tuple """
c = 1, 2, 3

print(a)
print(b)
print(c)



